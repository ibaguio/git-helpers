#!/bin/bash
# @author ibaguio
# the table below is generated by https://ozh.github.io/ascii-tables/
# table source:
# Command / Helper Name   Description Shorthand
# add-staged  Add already staged files again  g as, g asp
#     Use `asp` as a shortcut to `g as -p`
#
# get-branch  Looks for a branch given a ticket number    g gb <ticket num>
#
# checkout-x  Look for and checkout a branch given ticket number  g co <ticket num>
# checkout -b Create a new branch g cob
#
# diff --cached   Show diff of staged files   g dc
# diffc   Show diff of a given hash   g diffc <hash>
#
# fixup   Fixups each staged file to their last commit    g fup, g fupr
#     Use `fupr` to rebase and squash the fixups
#
# reset --soft HEAD   Undo last commit, and put changes to staged g rsh
# reset HEAD + checkout   Reset and remove all changes to tracked files   g rhco

cat <<EOF
                   MMM.           .MMM
                   MMMMMMMMMMMMMMMMMMM
                   MMMMMMMMMMMMMMMMMMM      ________________________
                  MMMMMMMMMMMMMMMMMMMMM    |                        |
                 MMMMMMMMMMMMMMMMMMMMMMM   |  git custom shortcuts  |
                MMMMMMMMMMMMMMMMMMMMMMMM   |    @author ibaguio     |
                MMMM::- -:::::::- -::MMMM  |_  _____________________|
                 MM~:~ 00~:::::~ 00~:~MM     |/
            .. MMMMM::.00:::+:::.00::MMMMM ..
                  .MM::::: ._. :::::MM.
                     MMMM;:::::;MMMM
              -MM        MMMMMMM
              ^  M+     MMMMMMMMM
                  MMMMMMM MM MM MM
                       MM MM MM MM
                       MM MM MM MM
                    .~~MM~MM~MM~MM~~.
                 ~~~~MM:~MM~~~MM~:MM~~~~
                ~~~~~~==~==~~~==~==~~~~~~
                 ~~~~~~==~==~==~==~~~~~~
                     :~==~==~==~==~~

+-----------------------+----------------------------------------------------+-------------------+
| Command / Helper Name |                    Description                     |     Shorthand     |
+-----------------------+----------------------------------------------------+-------------------+
| add-staged            | Add already staged files again                     | g as, g asp       |
|                       | Use "asp" as a shortcut to "g as -p"               |                   |
|                       |                                                    |                   |
| get-branch            | Looks for a branch given a ticket number           | g gb <ticket num> |
|                       |                                                    |                   |
| checkout-x            | Look for and checkout a branch given ticket number | g co <ticket num> |
| checkout -b           | Create a new branch                                | g cob             |
|                       |                                                    |                   |
| diff --cached         | Show diff of staged files                          | g dc              |
| diffc                 | Show diff of a given hash                          | g diffc <hash>    |
|                       |                                                    |                   |
| fixup                 | Fixups each staged file to their last commit       | g fup, g fupr     |
|                       | Use "fupr" to rebase and squash the fixups         |                   |
|                       |                                                    |                   |
| reset --soft HEAD     | Undo last commit, and put changes to staged        | g rsh             |
| reset HEAD + checkout | Reset and remove all changes to tracked files      | g rhco            |
+-----------------------+----------------------------------------------------+-------------------+
EOF
